export enum Conexts {
  TransactionHistory = `transaction-history-context`,
  FundExplorer = `fund-explorer-context`,
  PortfolioValuation = `portfolio-valuation-context`,
  MainChoice = `main-choice-context`,
}